# Usar la imagen oficial de Node.js con la versión LTS
FROM node

# Establecer el directorio de trabajo dentro del contenedor
WORKDIR /app

# Exponer el puerto en el contenedor
EXPOSE 3002

# Comando para iniciar la aplicación
CMD ["npm", "start"]
